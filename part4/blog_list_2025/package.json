{
	"name": "blog_list_2025",
	"version": "1.0.0",
	"description": "blog list for favourite websites",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node index.js",
		"dev": "cross-env NODE_ENV=development node --watch index.js",
		"test": "cross-env NODE_ENV=test node --test",
		"start:test": "cross-env NODE_ENV=test node --watch index.js"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bcrypt": "^5.1.1",
		"cross-env": "^7.0.3",
		"dotenv": "^16.4.7",
		"express": "^4.21.2",
		"express-async-errors": "^3.1.1",
		"jsonwebtoken": "^9.0.2",
		"mongoose": "^8.12.1"
	},
	"type": "module",
	"devDependencies": {
		"supertest": "^7.1.0"
	}
}
