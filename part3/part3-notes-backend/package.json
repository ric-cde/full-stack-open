{
	"name": "notebackend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon index.js",
		"start": "node index.js",
		"build:ui": "@powershell if (Test-Path dist) { Remove-Item -Recurse -Force dist } && cd ../part2-notes-frontend/ && npm run build && @powershell Copy-Item dist -Recurse ../part3-notes-backend/dist",
		"deploy": "fly deploy",
		"deploy:full": "npm run build:ui && npm run deploy",
		"lint": "eslint ."
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.4.7",
		"express": "^4.18.2",
		"mongoose": "^8.12.1"
	},
	"devDependencies": {
		"@eslint/js": "^9.22.0",
		"@flydotio/dockerfile": "^0.7.8",
		"@stylistic/eslint-plugin-js": "^4.2.0",
		"eslint": "^9.22.0",
		"nodemon": "^3.0.3",
		"prettier": "^3.5.3"
	},
	"type": "module"
}
